# VADRIVO — Cinematic React Experience

Premium animation-first static website for VADRIVO.

## Run
npm install
npm run dev

## Build
npm run build

The contact form is frontend-only and opens the visitor's mail client with a prefilled project request. No backend/database is used.

The world map is a static visual treatment; it does not use live location data.
